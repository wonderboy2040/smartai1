version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "3000:80"
    environment:
      - NODE_VERSION=22
    # Single service: ML runs in-process in Node.js, no Python service needed
    # Set env vars in Render dashboard or .env file
